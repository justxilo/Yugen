/**
 * AniKoto Stream Provider Placeholder (Frontend).
 * 
 * Note: Per PROJECT.md architecture, the frontend communicates strictly
 * with the FastAPI backend gateway. This file serves as a placeholder interface
 * for future contract definitions if needed. Direct connections to AniKoto from
 * the browser are strictly forbidden.
 */

export interface AniKotoProviderPlaceholder {
  readonly providerName: 'AniKoto';
}

export const ANIKOTO_PROVIDER_NAME = 'AniKoto';
